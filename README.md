# DagWeaver

Library for Apache Airflow DAG development.

## Installation

```bash
pip install dagweaver
```

## Development

```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -e ".[dev]"
pytest
```
